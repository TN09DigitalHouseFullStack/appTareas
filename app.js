const funcionesDeTareas = require ("./funciones/funcionesDeTareas");
funcionesDeTareas();

/*const tareasJSON = require('./leerTareas');

//Obtengo la palabra que necesito en la ejecucion de node app.js listar
let accion = process.argv[??];
// console.log(accion);

//Realizo la evaluacion de casos
switch (accion) {
    case 'listar':
        //Ciclar e imprimir las tareas con el formato #. titulo - estado

        break;
    case 'listarTerminadas':
        //Igual a listar solo que ahora solo listo las tareas terminadas

        break;
    case undefined:
        //Inidicarle al usuario adicionalmente las acciones disponibles
        console.log('Atencion debe ingresar una accion');
        break;
    default:
        //Inidicarle al usuario adicionalmente las acciones disponibles
        console.log('No entiendo');
        break;
}

// listar

// listarProgreso

// listarTerminadas

// listarPenndiente*/
